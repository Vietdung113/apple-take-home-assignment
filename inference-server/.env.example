# llama.cpp inference server configuration
PORT=8100
MODEL_REPO=unsloth/Qwen3-0.6B-GGUF
MODEL_FILE=Qwen3-0.6B-Q4_K_M.gguf
CTX_SIZE=4096
PARALLEL=4
GPU_LAYERS=-1

# For finetuned merged GGUF:
# MODEL_PATH=/path/to/finetuned-model.gguf

# For NVIDIA GPU (use docker-compose.yaml instead):
# See docker-compose.yaml for vLLM setup
