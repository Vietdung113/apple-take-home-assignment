[project]
name = "eval"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "datasets",
    "httpx",
    "jinja2>=3.1.6",
    "numpy",
    "pandas",
    "python-dotenv",
    "rouge-score",
    "sentence-transformers>=5.2.3",
    "tqdm",
    "transformers>=5.3.0",
]
